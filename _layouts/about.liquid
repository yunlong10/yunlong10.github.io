---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.title == 'blank' %}
        <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
        {{ site.last_name }}
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>
    <p class="desc">{{ page.subtitle }}</p>
  </header>

  <article>
    {% if page.profile %}
      <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% if page.profile.image_circular %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
          {% else %}
            {% assign profile_image_class = 'img-fluid z-depth-1
      rounded' %}
          {% endif %}
          {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
      30vw, 95vw"{% endcapture %}
          {%
            include figure.liquid loading="eager" fetchpriority="high" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
            cache_bust=true
          %}
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
      </div>
    {% endif %}

    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.news and site.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">News</a>
      </h2>
      {% include news.liquid limit=true %}
      <a href="news">👉 More news</a>
      <br>
      <br>
    {% endif %}

    <!-- Latest posts -->
    {% if site.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>Selected Research</h2>
      <div
        class="selected-research-note-row"
        style="display: flex; flex-wrap: wrap; align-items: center; gap: 0.35rem 0.65rem; font-size: 14px; margin: 0 0 0.75rem 0;"
      >
        <span>* Equal Contribution</span>
        <span style="color: var(--global-text-color-light, #888); user-select: none;" aria-hidden="true">|</span>
        <span
          class="selected-pubs-toolbar"
          style="display: inline-flex; flex-wrap: wrap; align-items: center; gap: 0.35rem 0.5rem;"
        >
          <label for="pub-sort-by" class="mb-0" style="font-size: 14px;">Sort by</label>
          <select
            id="pub-sort-by"
            class="form-control form-control-sm"
            style="width: auto; min-width: 9rem;"
            aria-label="Sort selected publications"
          >
            <option value="cites" selected>-- Please Select --</option>
            <option value="cites">Citations</option>
            <option value="stars">GitHub Stars</option>
            <option value="year">Year</option>
          </select>
        </span>
      </div>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- Social -->
    <h2>Misc.</h2>
    <br>
    <h5>Fun Facts</h5>
    <ul>
      <li>🩵 Preferred names: Yolo (en) or 芸珑 (zh).</li>
      <li>🍭 I’m into ACG, J-pop, singing, and cosplay.</li>
      <li>😝 Maybe I’m just cosplaying as a researcher—who knows?</li>
    </ul>

    <h5>Visitor Map</h5>
    <div
      class="visitor-map"
      id="clustrmaps-host"
      data-clustrmaps-src="https://clustrmaps.com/map_v2.js?cl=ffffff&d=mErswMtIf2K7G41Iql-K1paY9dnbAA1mf0bzUzgiHAs&co=5a9bd4&cmo=b83d7f&cmn=ff6ba3&w=350"
    ></div>
    <script>
      (function () {
        var host = document.getElementById('clustrmaps-host');
        if (!host || host.dataset.loaded === '1') return;
        function loadClustrmaps() {
          if (host.dataset.loaded === '1') return;
          host.dataset.loaded = '1';
          var s = document.createElement('script');
          s.async = true;
          s.id = 'clustrmaps';
          s.src = host.getAttribute('data-clustrmaps-src');
          host.appendChild(s);
        }
        if (!('IntersectionObserver' in window)) {
          loadClustrmaps();
          return;
        }
        var io = new IntersectionObserver(
          function (entries) {
            entries.forEach(function (e) {
              if (e.isIntersecting) {
                loadClustrmaps();
                io.disconnect();
              }
            });
          },
          { rootMargin: '120px' }
        );
        io.observe(host);
      })();
    </script>
    <div style="color: var(--global-subtitle-color); margin-top: 0px; opacity: 0.85; white-space: nowrap; text-align: center; font-size: 0.9em; width: 100%; max-width: 100%;">
      <span>Thank you for your visit!</span>
    </div>

    <h5 id="social-links">Social Links</h5>

    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>

        {% if site.contact_note %}
          <div class="contact-note">{{ site.contact_note }}</div>
        {% endif %}
      </div>
    {% endif %}

    {% if site.newsletter.enabled and site.footer_fixed %}
      {% include scripts/newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>
